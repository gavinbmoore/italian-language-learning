name = "italian-learning-api"
main = "src/server.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

[vars]
NODE_ENV = "production"
FIREBASE_PROJECT_ID = "demo-project"

# DATABASE_URL should be set as a secret using: wrangler secret put DATABASE_URL
